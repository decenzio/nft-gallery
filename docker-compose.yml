version: "3.8"
services:
  frontend:
    container_name: Kodadot
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "9090:9090"
    env_file: .env
    command: pnpm start:node
